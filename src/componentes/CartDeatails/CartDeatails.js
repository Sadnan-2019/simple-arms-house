import React from 'react';

const CartDeatails = (props) => {
          console.log(props.cart)

          return (
                    <div>
                              
                    </div>
          );
};

export default CartDeatails;